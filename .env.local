# Shared / reference
NODE_ENV=development

# API gateway (apps/api)
API_PORT=4000
LQE_WORKER_URL=http://localhost:8000
API_KEY=dev-secret-key-123

# Frontend (apps/web)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Python worker (services/lqe-worker)
LQE_WORKER_PORT=8000
PY_ENV=development
